services:
  - type: web
    name: route-planner
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run render:start
    envVars:
      - key: DATABASE_URL
        value: file:./dev.db
      - key: NODE_VERSION
        value: 20
